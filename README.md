# L0 : Демонстрационный сервис с Kafka, PostgreSQL, кешем

Микросервисный проект на **Go**, предназначенный для обработки заказов:  
один сервис принимает заказы из Kafka и сохраняет их в PostgreSQL,  
другой - генерирует и отправляет тестовые заказы.

---

## 📂 Структура проекта

```
l0/
├── cmd/
│   ├── order-service/        # основной сервис обработки заказов
│   │   └── main.go
│   └── producer/             # продюсер для Kafka
│       └── main.go
├── deploy/
│   ├── .env                  # переменные окружения
│   ├── docker-compose.yml    # оркестрация сервисов
│   ├── Dockerfile.prdc       # сборка продюсера
│   └── Dockerfile.srvc       # сборка сервиса заказов
├── internal/
│   ├── cache/                # in-memory cache
│   │   └── cache.go
│   ├── handlers/             # http-хэндлеры (api/order, index.html)
│   │   └── handlers.go
│   ├── kafka/                # consumer/producer Kafka
│   │   └── kafka.go
│   ├── models/               # структуры заказов
│   │   └── models.go
│   └── service/              # бизнес-логика
│       ├── service.go
│       └── validation.go
├── migrations/               # SQL-миграции для БД
├── web/
│   └── index.html            # простая витрина заказов
├── go.mod
└── go.sum
```

---

## ⚙️ Основные компоненты

### 🧩 Order Service (`cmd/order-service`)
- Поднимает HTTP-сервер на `:8080`.
- Роуты:
  - `/order/` - отдает `index.html`.
  - `/api/order/{id}` - возвращает заказ по ID из кэша или БД.
- Подключается к Kafka и получает заказы из топика `orders`.
- После получения - валидирует JSON, пишет в БД, сохраняет в кэш.

### 🚀 Producer (`cmd/producer`)
- Отправляет фейковые заказы в топик `orders`.
- Используется для тестирования всей цепочки Kafka → БД → HTTP.

### 🗃️ PostgreSQL
- Таблицы `orders`, `deliveries`, `payments`, `items`.
- Миграции выполняются автоматически контейнером `migrator`.

### 💬 Kafka
- Один брокер (Bitnami Kafka).
- Автоматически создаётся топик `orders`.

---

## 🐳 Запуск через Docker Compose

1.В папке `deploy` есть файл `.env.example`, измени его под себя, и      переименуй в `.env`   

2. Запуск всех сервисов:
   ```bash
   docker compose -f deploy/docker-compose.yml up --build
   ```

3. После старта:
   - **Order Service** доступен по адресу [http://localhost:8080/order](http://localhost:8080/order)
   - **Kafka** - на порту `9092`
   - **PostgreSQL** - на порту `5434` (локально)

---

## 📦 Используемые пакеты Go

### 🧰 Стандартная библиотека
| Пакет | Назначение |
|:--|:--|
| **net/http** | HTTP-сервер и маршрутизация запросов (`handlers.go`, `main.go`) |
| **encoding/json** | Сериализация/десериализация JSON заказов |
| **context** | Контекст для graceful shutdown и обработки сигналов |
| **database/sql** | Работа с PostgreSQL через драйвер |
| **sync** | Мьютексы, WaitGroup в `cache` и `main.go` |
| **time** | TTL-кэш, даты заказов |
| **log** | Логирование ошибок и статуса |
| **os / os/signal / syscall** | Перехват SIGINT/SIGTERM для корректного завершения |
| **fmt / errors** | Форматирование и генерация ошибок |
| **strings / regexp / unicode** | Проверка валидности данных при валидации (`validation.go`) |

### 🌐 Внешние библиотеки
| Пакет | Назначение |
|:--|:--|
| **github.com/lib/pq** | Драйвер PostgreSQL |
| **github.com/segmentio/kafka-go** | Работа с Kafka: producer и consumer |
| **github.com/gorilla/mux** | Удобная маршрутизация HTTP
| **github.com/brianvoe/gofakeit/v7** | Генерация фейковых заказов

---

## 📦 Миграции

Контейнер **`migrator`** автоматически накатывает все миграции при запуске.

Для ручного отката на один шаг:
```bash
docker compose run --rm migrator-down
```

---

## 🧠 Основная логика

1. **Producer** создаёт JSON заказа и публикует в `orders`.
2. **Order Service (Consumer)** получает сообщение, валидирует и сохраняет в БД.
3. Данные кэшируются в памяти (`internal/cache`).
4. При запросе `GET /api/order/{id}`:
   - если есть в кэше → отдает сразу;
   - если нет → достает из БД и добавляет в кэш.

---

## 🧩 Архитектура

```
Producer → Kafka(topic: orders) → Order Service → PostgreSQL
                                        ↓
                                    In-Memory Cache
                                        ↓
                                 HTTP API (GET /order/{id})
```

---

## 💡 Пример запроса

```bash
curl http://localhost:8080/api/order/1
```

Ответ:
```json
{
  "order_uid": "12345",
  "track_number": "WBILMTESTTRACK",
  "delivery": {
    "name": "John Doe",
    "phone": "+79999999999"
  },
  "payment": {
    "amount": 1500,
    "currency": "RUB"
  },
  "items": [
    {"chrt_id": 1, "price": 1000},
    {"chrt_id": 2, "price": 500}
  ]
}
```

---

## 🧹 Завершение работы

Остановить все контейнеры:
```bash
docker compose -f deploy/docker-compose.yml down
```

Удалить тома и сети:
```bash
docker compose -f deploy/docker-compose.yml down -v --remove-orphans
```

---

## 🛠️ Технологии

- **Go 1.25.1**
- **PostgreSQL 15**
- **Kafka (Bitnami)**
- **Docker Compose**
- **migrate/migrate**
- **HTML + JS (web/index.html)**

---

## 🔍 Автор и структура кода

Код полностью в папке `internal/`, каждая часть изолирована по ответственности:
- `models` - структуры заказов и вложенных сущностей  
- `cache` - локальное хранение заказов  
- `service` - взаимодействие с БД, валидация  
- `handlers` - REST API  
- `kafka` - consumer / producer  
- `cmd/order-service` и `cmd/producer` - точки входа  

---

## ✅ Проверка

После запуска:
1. Подожди логи `"Топик orders готов"` и `"order-service started"`.
2. Зайди в браузере на [http://localhost:8080/order](http://localhost:8080/order).
3. Вставь ID заказа (например `1`) и проверь вывод JSON.

---

_Проект готов к локальному и контейнерному запуску.  
Легко расширяется для добавления новых событий и сервисов._
